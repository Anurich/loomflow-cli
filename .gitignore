# Secrets — never commit
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/

# Tooling caches
.pytest_cache/
.ruff_cache/

# Browser-operator (/computer) runtime artifacts — page snapshots,
# console logs, screenshots. Not source.
.playwright-mcp/
last_page.png
.mypy_cache/

# loom-code per-project runtime state + generated indices
# (.loom = notebook + memory db + graphify graph; graphify-out =
# graphify's AST cache; LOOM.md = the loominit annotation output).
# These are produced when loom-code runs on THIS repo (dogfooding
# /loominit) and must never be committed.
.loom/
graphify-out/
/LOOM.md

# OS
.DS_Store
